# MinIO Configuration
# The endpoint of your MinIO cluster (hostname:port)
# Examples:
#   - play.min.io:9000 (default for development)
#   - localhost:9000 (local development)
#   - minio.example.com:9000 (production)
MINIO_ENDPOINT=play.min.io:9000
